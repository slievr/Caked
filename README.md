# Caked
Simple CRUD app for those absessed with all things cake.

intalation:

   webpack:
      npm install --global webpack
      
   caked:
      npm install 
      npm run build
      cordova run browser
